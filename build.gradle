buildscript {
    apply from: 'dependencies.gradle'
}

subprojects {
    buildscript {
        repositories {
            google()
            jcenter()
        }
    }
    repositories {
        google()
        jcenter()
    }
}
